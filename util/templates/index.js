const component = require('./component');
const componentStyles = require('./component-styled');
const componentTypes = require('./component.types');

module.exports = [
  component,
  componentTypes,
  componentStyles
];